name: Improved GCC Problem Matcher

description: Get annotations for warnings and errors on builds using gcc

author: Ole Morud

branding:
  icon: search
  color: yellow

inputs:
  build-directory:
    description: 'base directory for build, e.g. /workdir/build'
    required: false
  skip-directories:
    description: 'directories to be skipped, e.g. subprojects|external'
    required: false

runs:
  using: 'node24'
  main: 'dist/index.js'
